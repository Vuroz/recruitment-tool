"use client";

import type React from "react";

export default function MainViewRecruiter() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-center bg-gradient-to-b from-[#026e6e] to-[#152a2b] text-white">

        </main>
    );
}